import numpy as np
import cv2 as cv


def sparse_mapping(pose_0: np.ndarray,
                   instrinsic_0: np.ndarray,
                   image_0: np.ndarray,
                   pose_1: np.ndarray,
                   instrinsic_1: np.ndarray,
                   image_1: np.ndarray,
                   match: dict) -> None:
    ()
