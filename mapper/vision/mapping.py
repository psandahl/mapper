import numpy as np
import cv2 as cv


def sparse_mapping(pose0: np.ndarray,
                   instrinsic0: np.ndarray,
                   image0: np.ndarray,
                   pose1: np.ndarray,
                   instrinsic1: np.ndarray,
                   image1: np.ndarray,
                   match: dict) -> None:
    ()
